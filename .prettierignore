node_modules
dist
build
coverage
package-lock.json

# Legacy, being deleted module by module — reformatting it would bury the moves
apps/api/src-old

# Hand-written CSS that the PDF renderer depends on byte-for-byte
apps/api/src/modules/reportcards/templates

# Same reason: the certificate templates carry hand-written inline CSS that the
# Puppeteer output depends on exactly as written. Whole directory, not just the
# renderer, so a future template file is covered the day it lands.
apps/api/src/modules/documents/lib

apps/api/uploads
apps/api/public
